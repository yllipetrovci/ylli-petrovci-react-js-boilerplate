import React from 'react';
import '../styles/components/select.scss';

const Select = (props)=>{
    return(
        <select className="select-component">
            <option value="option">Type: Sort</option>
        </select>
    );
};

export default Select;